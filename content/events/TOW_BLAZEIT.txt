namespace = TOW_leader_start_traits

country_event = {
	id = TOW_leader_start_traits.420
	hide_window = no
	title = TOW_LST.SAVEMEMOONMAN_TITLE
	desc = TOW_LST.SAVEMEMOONMAN_DESC
	picture = GFX_evt_throne_room
  show_sound = event_administrative_work
	is_triggered_only = yes
	immediate = {}

option = {
  name = TOW_LST.ARCHSEN
  custom_tooltip = TOW_LST.ARCHSEN_DESC
  trigger = {
    leader = {
      leader_class = ruler
      AND = { NOT = { has_trait = trait_ruler_architectural_sense } }
    }
  }
  hidden_effect = {
  country_event = { id = TOW_leader_start_traits.1}
  every_owned_leader = {
      limit ={ leader_class = ruler }
      add_trait = trait_ruler_architectural_sense
          }
      }
  }

option = {
  name = TOW_LST.CHARI
  custom_tooltip = TOW_LST.CHARI_DESC
  trigger = {
    leader = {
      leader_class = ruler
      AND = { NOT = { has_trait = trait_ruler_charismatic } }
    }
  }
  hidden_effect = {
  country_event = { id = TOW_leader_start_traits.1}
  every_owned_leader = {
      limit ={ leader_class = ruler }
      add_trait = trait_ruler_charismatic
          }
      }
  }

option = {
  name = TOW_LST.CHAMP_PEEP
  custom_tooltip = TOW_LST.CHAMP_PEEP_DESC
  trigger = {
    leader = {
      leader_class = ruler
      AND = { NOT = { has_trait = trait_ruler_champion_of_the_people } }
    }
  }
  hidden_effect = {
  country_event = { id = TOW_leader_start_traits.1}
  every_owned_leader = {
      limit ={ leader_class = ruler }
      add_trait = trait_ruler_champion_of_the_people
          }
      }
  }

option = {
  name = TOW_LST.DEEP_CON
  custom_tooltip = TOW_LST.DEEP_CON_DESC
  trigger = {
    leader = {
      leader_class = ruler
      AND = { NOT = { has_trait = trait_ruler_deep_connections } }
    }
  }
  hidden_effect = {
  country_event = { id = TOW_leader_start_traits.1}
  every_owned_leader = {
      limit ={ leader_class = ruler }
      add_trait = trait_ruler_deep_connections
          }
      }
  }

option = {
  name = TOW_LST.EXPAN
  custom_tooltip = TOW_LST.EXPAN_DESC
  trigger = {
    leader = {
      leader_class = ruler
      AND = { NOT = { has_trait = trait_ruler_expansionist } }
    }
  }
  hidden_effect = {
  country_event = { id = TOW_leader_start_traits.1}
  every_owned_leader = {
      limit ={ leader_class = ruler }
      add_trait = trait_ruler_expansionist
          }
      }
  }

option = {
  name = TOW_LST.EXPLORER
  custom_tooltip = TOW_LST.EXPLORER_DESC
  trigger = {
    leader = {
      leader_class = ruler
      AND = { NOT = { has_trait = trait_ruler_explorer } }
    }
  }
  hidden_effect = {
  country_event = { id = TOW_leader_start_traits.1}
  every_owned_leader = {
      limit ={ leader_class = ruler }
      add_trait = trait_ruler_explorer
          }
      }
  }

option = {
  name = TOW_LST.FER_PREACH
  custom_tooltip = TOW_LST.FER_PREACH_DESC
  trigger = {
    leader = {
      leader_class = ruler
      AND = { NOT = { has_trait = trait_ruler_fertility_preacher } }
    }
  }
  hidden_effect = {
  country_event = { id = TOW_leader_start_traits.1}
  every_owned_leader = {
      limit ={ leader_class = ruler }
      add_trait = trait_ruler_fertility_preacher
          }
      }
  }

option = {
  name = TOW_LST.HOMESKY
  custom_tooltip = TOW_LST.HOMESKY_DESC
  trigger = {
    leader = {
      leader_class = ruler
      AND = { NOT = { has_trait = trait_ruler_home_in_the_sky } }
    }
  }
  hidden_effect = {
  country_event = { id = TOW_leader_start_traits.1}
  every_owned_leader = {
      limit ={ leader_class = ruler }
      add_trait = trait_ruler_home_in_the_sky
          }
      }
  }

option = {
  name = TOW_LST.INDUST
  custom_tooltip = TOW_LST.INDUST_DESC
  trigger = {
    leader = {
      leader_class = ruler
      AND = { NOT = { has_trait = trait_ruler_industrialist } }
    }
  }
  hidden_effect = {
  country_event = { id = TOW_leader_start_traits.1}
  every_owned_leader = {
      limit ={ leader_class = ruler }
      add_trait = trait_ruler_industrialist
          }
      }
  }

option = {
  name = TOW_LST.INVESTOR
  custom_tooltip = TOW_LST.INVESTOR_DESC
  trigger = {
    leader = {
      leader_class = ruler
      AND = { NOT = { has_trait = trait_ruler_investor } }
    }
  }
  hidden_effect = {
  country_event = { id = TOW_leader_start_traits.1}
  every_owned_leader = {
      limit ={ leader_class = ruler }
      add_trait = trait_ruler_investor
          }
      }
  }

  option = {
  name = TOW_LST.SPACE_MINER
  custom_tooltip = TOW_LST.SPACE_MINER_DESC
  trigger = {
    leader = {
      leader_class = ruler
      AND = { NOT = { has_trait = trait_ruler_space_miner } }
    }
  }
  hidden_effect = {
  country_event = { id = TOW_leader_start_traits.1}
  every_owned_leader = {
      limit ={ leader_class = ruler }
      add_trait = trait_ruler_space_miner
          }
      }
  }

option = {
  name = TOW_LST.WARL
  custom_tooltip = TOW_LST.WARL_DESC
  trigger = {
    leader = {
      leader_class = ruler
      AND = { NOT = { has_trait = trait_ruler_warlike } }
    }
  }
  hidden_effect = {
  country_event = { id = TOW_leader_start_traits.1}
  every_owned_leader = {
      limit ={ leader_class = ruler }
      add_trait = trait_ruler_warlike
        }
    }
}
####
option = {
  name = "TOW_LST.ADAPTABLE"
  custom_tooltip = "TOW_LST.ADAPTABLE_DESC"
  trigger = {
    leader = {
      leader_class = ruler
      AND = { NOT = { has_trait = "leader_trait_adaptable" } }
    }
  }
  hidden_effect = {
  country_event = { id = TOW_leader_start_traits.1}
  every_owned_leader = {
      limit ={ leader_class = ruler }
      add_trait = leader_trait_adaptable
        }
    }
}

option = {
  name = "TOW_LST.RESILIENT"
  custom_tooltip = "TOW_LST.RESILIENT_DESC"
  trigger = {
    leader = {
      leader_class = ruler
      AND = { NOT = { has_trait = "leader_trait_resilient" } }
    }
  }
  hidden_effect = {
  country_event = { id = TOW_leader_start_traits.1}
  every_owned_leader = {
      limit ={ leader_class = ruler }
      add_trait = leader_trait_resilient
        }
    }
}

option = {
  name = "TOW_LST.EAGER"
  custom_tooltip = "TOW_LST.EAGER_DESC"
  trigger = {
    leader = {
      leader_class = ruler
      AND = { NOT = { has_trait = "leader_trait_eager" } }
    }
  }
  hidden_effect = {
  country_event = { id = TOW_leader_start_traits.1}
  every_owned_leader = {
      limit ={ leader_class = ruler }
      add_trait = leader_trait_eager
        }
    }
}


	option ={
		name = TOW_LST.NOPENOPENOPE
		hidden_effect = {
				country_event = { id = TOW_leader_start_traits.666}
		}
	}
}
##############################################
country_event = {
	id = TOW_leader_start_traits.421
	hide_window = no
	title = TOW_LST.MOMMYGIBMILK
	desc = TOW_LST.MOMMYGIBMILK_DESC
	picture = GFX_evt_throne_room
  show_sound = event_administrative_work
	is_triggered_only = yes
	immediate = {}

option = {
  name = "TOW_LST.NEW_BOOT"
  custom_tooltip = "TOW_LST.NEW_BOOT_DESC"
  trigger = {
    leader = {
      leader_class = ruler
      AND = { NOT = { has_trait = "leader_trait_newboot" } }
    }
  }
  hidden_effect = {
  country_event = { id = TOW_leader_start_traits.1}
  every_owned_leader = {
      limit ={ leader_class = ruler }
      add_trait = leader_trait_newboot
        }
    }
}

option = {
  name = "TOW_LST.FLEXIBLE_PROGRAMMING"
  custom_tooltip = "TOW_LST.FLEXIBLE_PROGRAMMING_DESC"
  trigger = {
    leader = {
      leader_class = ruler
      AND = { NOT = { has_trait = "leader_trait_flexible_programming" } }
    }
  }
  hidden_effect = {
  country_event = { id = TOW_leader_start_traits.1}
  every_owned_leader = {
      limit ={ leader_class = ruler }
      add_trait = leader_trait_flexible_programming
        }
    }
}

option = {
  name = TOW_LST.IUSEPOL
  custom_tooltip = TOW_LST.IUSEPOL_DESC
  trigger = {
    leader = {
      leader_class = ruler
      AND = { NOT = { has_trait = leader_trait_arrested_development } }
    }
  }
  hidden_effect = {
  country_event = { id = TOW_leader_start_traits.1}
  every_owned_leader = {
      limit ={ leader_class = ruler }
      add_trait = leader_trait_arrested_development
          }
      }
  }

option = {
  name = TOW_LST.HIGHONLSD
  custom_tooltip = TOW_LST.HIGHONLSD_DESC
  trigger = {
    leader = {
      leader_class = ruler
      AND = { NOT = { has_trait = leader_trait_substance_abuser } }
    }
  }
  hidden_effect = {
  country_event = { id = TOW_leader_start_traits.1}
  every_owned_leader = {
      limit ={ leader_class = ruler }
      add_trait = leader_trait_substance_abuser
          }
      }
  }

option = {
  name = TOW_LST.WEEDMANTRUDEAU
  custom_tooltip = TOW_LST.WEEDMANTRUDEAU_DESC
  trigger = {
    leader = {
      leader_class = ruler
      AND = { NOT = { has_trait = leader_trait_rigid_programming } }
    }
  }
  hidden_effect = {
  country_event = { id = TOW_leader_start_traits.1}
  every_owned_leader = {
      limit ={ leader_class = ruler }
      add_trait = leader_trait_rigid_programming
          }
      }
  }

option = {
  name = TOW_LST.FUCKITWEWILLDOITLIVE
  custom_tooltip = TOW_LST.FUCKITWEWILLDOITLIVE_DESC
  trigger = {
    leader = {
      leader_class = ruler
      AND = { NOT = { has_trait = leader_trait_stubborn } }
    }
  }
  hidden_effect = {
  country_event = { id = TOW_leader_start_traits.1}
  every_owned_leader = {
      limit ={ leader_class = ruler }
      add_trait = leader_trait_stubborn
          }
      }
  }

	option ={
		name = TOW_LST.NOPENOPENOPE
		hidden_effect = {
				country_event = { id = TOW_leader_start_traits.666}
		}
}

}
####################################
country_event = {
	id = TOW_leader_start_traits.422
	hide_window = no
	title = TOW_LST.CHEATINGBAGUETTE
	desc = TOW_LST.CHEATINGBAGUETTE_DESC
	picture = GFX_evt_throne_room
  show_sound = event_administrative_work
	is_triggered_only = yes
	immediate = {}

option = {
  name = TOW_LST.REFORMER
  custom_tooltip = TOW_LST.REFORMER_DESC
  trigger = {
    leader = {
      leader_class = ruler
      AND = { NOT = { has_trait = leader_trait_reformer } }
    }
  }
  hidden_effect = {
  country_event = { id = TOW_leader_start_traits.1}
  every_owned_leader = {
      limit ={ leader_class = ruler }
      add_trait = leader_trait_reformer
          }
      }
  }

option = {
  name = "TOW_LST.SHROUD"
  custom_tooltip = "TOW_LST.SHROUD_DESC"
  trigger = {
    leader = {
      leader_class = ruler
      AND = { NOT = { has_trait = "leader_trait_shroud_age" } }
    }
  }
  hidden_effect = {
  country_event = { id = TOW_leader_start_traits.1}
  every_owned_leader = {
      limit ={ leader_class = ruler }
      add_trait = leader_trait_shroud_age
        }
    }
}

option = {
  name = "TOW_LST.HIVE_MIND"
  custom_tooltip = "TOW_LST.HIVE_MIND_DESC"
  trigger = {
    leader = {
      leader_class = ruler
      AND = { NOT = { has_trait = "leader_trait_ruler_hive_mind" } }
    }
  }
  hidden_effect = {
  country_event = { id = TOW_leader_start_traits.1}
  every_owned_leader = {
      limit ={ leader_class = ruler }
      add_trait = leader_trait_ruler_hive_mind
        }
    }
}

option = {
  name = "TOW_LST.MACHINE_INTELLIGENCE"
  custom_tooltip = "TOW_LST.MACHINE_INTELLIGENCE_DESC"
  trigger = {
    leader = {
      leader_class = ruler
      AND = { NOT = { has_trait = "leader_trait_ruler_machine_intelligence" } }
    }
  }
  hidden_effect = {
  country_event = { id = TOW_leader_start_traits.1}
  every_owned_leader = {
      limit ={ leader_class = ruler }
      add_trait = leader_trait_ruler_machine_intelligence
        }
    }
}

option = {
  name = "TOW_LST.WORLD_SHAPER"
  custom_tooltip = "TOW_LST.WORLD_SHAPER_DESC"
  trigger = {
    leader = {
      leader_class = ruler
      AND = { NOT = { has_trait = "trait_ruler_world_shaper" } }
    }
  }
  hidden_effect = {
  country_event = { id = TOW_leader_start_traits.1}
  every_owned_leader = {
      limit ={ leader_class = ruler }
      add_trait = trait_ruler_world_shaper
        }
    }
}

option = {
  name = "TOW_LST.RECRUITER"
  custom_tooltip = "TOW_LST.RECRUITER_DESC"
  trigger = {
    leader = {
      leader_class = ruler
      AND = { NOT = { has_trait = "trait_ruler_recruiter" } }
    }
  }
  hidden_effect = {
  country_event = { id = TOW_leader_start_traits.1}
  every_owned_leader = {
      limit ={ leader_class = ruler }
      add_trait = trait_ruler_recruiter
        }
    }
}

option = {
  name = "TOW_LST.MILITARY_PIONEER"
  custom_tooltip = "TOW_LST.MILITARY_PIONEER_DESC"
  trigger = {
    leader = {
      leader_class = ruler
      AND = { NOT = { has_trait = "trait_ruler_military_pioneer" } }
    }
  }
  hidden_effect = {
  country_event = { id = TOW_leader_start_traits.1}
  every_owned_leader = {
      limit ={ leader_class = ruler }
      add_trait = trait_ruler_military_pioneer
        }
    }
}

option = {
  name = "TOW_LST.LOGISTIC_UNDERSTANDING"
  custom_tooltip = "TOW_LST.LOGISTIC_UNDERSTANDING_DESC"
  trigger = {
    leader = {
      leader_class = ruler
      AND = { NOT = { has_trait = "trait_ruler_logistic_understanding" } }
    }
  }
  hidden_effect = {
  country_event = { id = TOW_leader_start_traits.1}
  every_owned_leader = {
      limit ={ leader_class = ruler }
      add_trait = trait_ruler_logistic_understanding
        }
    }
}

option = {
  name = "TOW_LST.FRONTIER_SPIRIT"
  custom_tooltip = "TOW_LST.FRONTIER_SPIRIT_DESC"
  trigger = {
    leader = {
      leader_class = ruler
      AND = { NOT = { has_trait = "trait_ruler_frontier_spirit" } }
    }
  }
  hidden_effect = {
  country_event = { id = TOW_leader_start_traits.1}
  every_owned_leader = {
      limit ={ leader_class = ruler }
      add_trait = trait_ruler_frontier_spirit
        }
    }
}

option = {
  name = "TOW_LST.FROM_THE_RANKS"
  custom_tooltip = "TOW_LST.FROM_THE_RANKS_DESC"
  trigger = {
    leader = {
      leader_class = ruler
      AND = { NOT = { has_trait = "trait_ruler_from_the_ranks" } }
    }
  }
  hidden_effect = {
  country_event = { id = TOW_leader_start_traits.1}
  every_owned_leader = {
      limit ={ leader_class = ruler }
      add_trait = trait_ruler_from_the_ranks
        }
    }
}

option = {
  name = "TOW_LST.FORTIFIER"
  custom_tooltip = "TOW_LST.FORTIFIER_DESC"
  trigger = {
    leader = {
      leader_class = ruler
      AND = { NOT = { has_trait = "trait_ruler_fortifier" } }
    }
  }
  hidden_effect = {
  country_event = { id = TOW_leader_start_traits.1}
  every_owned_leader = {
      limit ={ leader_class = ruler }
      add_trait = trait_ruler_fortifier
        }
    }
}

option = {
  name = "TOW_LST.FLEET_ORGANIZER"
  custom_tooltip = "TOW_LST.FLEET_ORGANIZER_DESC"
  trigger = {
    leader = {
      leader_class = ruler
      AND = { NOT = { has_trait = "leader_trait_fleet_organizer" } }
    }
  }
  hidden_effect = {
  country_event = { id = TOW_leader_start_traits.1}
  every_owned_leader = {
      limit ={ leader_class = ruler }
      add_trait = leader_trait_fleet_organizer
        }
    }
}

option = {
  name = "TOW_LST.EYE_FOR_TALENT"
  custom_tooltip = "TOW_LST.EYE_FOR_TALENT_DESC"
  trigger = {
    leader = {
      leader_class = ruler
      AND = { NOT = { has_trait = "trait_ruler_eye_for_talent" } }
    }
  }
  hidden_effect = {
  country_event = { id = TOW_leader_start_traits.1}
  every_owned_leader = {
      limit ={ leader_class = ruler }
      add_trait = trait_ruler_eye_for_talent
        }
    }
}

option = {
  name = "TOW_LST.CORFO"
  custom_tooltip = "TOW_LST.CORFO_DESC"
  trigger = {
    leader = {
      leader_class = ruler
      AND = { NOT = { has_trait = "trait_ruler_corvette_focus" } }
    }
  }
  hidden_effect = {
  country_event = { id = TOW_leader_start_traits.1}
  every_owned_leader = {
      limit ={ leader_class = ruler }
      add_trait = trait_ruler_corvette_focus
        }
    }
}

option = {
  name = "TOW_LST.DESTROYER_FOCUS"
  custom_tooltip = "TOW_LST.DESTROYER_FOCUS_DESC"
  trigger = {
    leader = {
      leader_class = ruler
      AND = { NOT = { has_trait = "trait_ruler_destroyer_focus" } }
    }
  }
  hidden_effect = {
  country_event = { id = TOW_leader_start_traits.1}
  every_owned_leader = {
      limit ={ leader_class = ruler }
      add_trait = trait_ruler_destroyer_focus
        }
    }
}

option = {
  name = "TOW_LST.CRUISER_FOCUS"
  custom_tooltip = "TOW_LST.CRUISER_FOCUS_DESC"
  trigger = {
    leader = {
      leader_class = ruler
      AND = { NOT = { has_trait = "trait_ruler_cruiser_focus" } }
    }
  }
  hidden_effect = {
  country_event = { id = TOW_leader_start_traits.1}
  every_owned_leader = {
      limit ={ leader_class = ruler }
      add_trait = trait_ruler_cruiser_focus
        }
    }
}

option = {
  name = "TOW_LST.BATTLESHIP_FOCUS"
  custom_tooltip = "TOW_LST.BATTLESHIP_FOCUS_DESC"
  trigger = {
    leader = {
      leader_class = ruler
      AND = { NOT = { has_trait = "trait_ruler_battleship_focus" } }
    }
  }
  hidden_effect = {
  country_event = { id = TOW_leader_start_traits.1}
  every_owned_leader = {
      limit ={ leader_class = ruler }
      add_trait = trait_ruler_battleship_focus
        }
    }
}

option = {
  name = "TOW_LST.CYBORG_BILLIE"
  custom_tooltip = "TOW_LST.CYBORG_BILLIE_DESC"
  trigger = {
    leader = {
      leader_class = ruler
      AND = { NOT = { has_trait = "leader_trait_ruler_cyborg" } }
    }
  }
  hidden_effect = {
  country_event = { id = TOW_leader_start_traits.1}
  every_owned_leader = {
      limit ={ leader_class = ruler }
      add_trait = leader_trait_ruler_cyborg
        }
    }
}

option = {
  name = "TOW_LST.SYNTHETIC_BILLIE"
  custom_tooltip = "TOW_LST.SYNTHETIC_BILLIE_DESC"
  trigger = {
    leader = {
      leader_class = ruler
      AND = { NOT = { has_trait = "leader_trait_ruler_synthetic" } }
    }
  }
  hidden_effect = {
  country_event = { id = TOW_leader_start_traits.1}
  every_owned_leader = {
      limit ={ leader_class = ruler }
      add_trait = leader_trait_ruler_synthetic
        }
    }
}

option = {
  name = "TOW_LST.PSYCHIC_BILLIE"
  custom_tooltip = "TOW_LST.PSYCHIC_BILLIE_DESC"
  trigger = {
    leader = {
      leader_class = ruler
      AND = { NOT = { has_trait = "leader_trait_ruler_psionic" } }
    }
  }
  hidden_effect = {
  country_event = { id = TOW_leader_start_traits.1}
  every_owned_leader = {
      limit ={ leader_class = ruler }
      add_trait = leader_trait_ruler_psionic
        }
    }
}

option = {
  name = "TOW_LST.CHOSEN_BILLIE"
  custom_tooltip = "TOW_LST.CHOSEN_BILLIE_DESC"
  trigger = {
    leader = {
      leader_class = ruler
      AND = { NOT = { has_trait = "leader_trait_ruler_chosen" } }
    }
  }
  hidden_effect = {
  country_event = { id = TOW_leader_start_traits.1}
  every_owned_leader = {
      limit ={ leader_class = ruler }
      add_trait = leader_trait_ruler_chosen
        }
    }
}

option = {
  name = "TOW_LST.ERUDITE_BILLIE"
  custom_tooltip = "TOW_LST.ERUDITE_BILLIE_DESC"
  trigger = {
    leader = {
      leader_class = ruler
      AND = { NOT = { has_trait = "leader_trait_ruler_erudite" } }
    }
  }
  hidden_effect = {
  country_event = { id = TOW_leader_start_traits.1}
  every_owned_leader = {
      limit ={ leader_class = ruler }
      add_trait = leader_trait_ruler_erudite
        }
    }
}

option = {
  name = "TOW_LST.BRAIN_SLUG_BILLIE"
  custom_tooltip = "TOW_LST.BRAIN_SLUG_BILLIE_DESC"
  trigger = {
    leader = {
      leader_class = ruler
      AND = { NOT = { has_trait = "leader_trait_ruler_brainslug" } }
    }
  }
  hidden_effect = {
  country_event = { id = TOW_leader_start_traits.1}
  every_owned_leader = {
      limit ={ leader_class = ruler }
      add_trait = leader_trait_ruler_brainslug
        }
    }
}

	option ={
		name = TOW_LST.NOPENOPENOPE
		hidden_effect = {
				country_event = { id = TOW_leader_start_traits.666}
		}
	}
}
